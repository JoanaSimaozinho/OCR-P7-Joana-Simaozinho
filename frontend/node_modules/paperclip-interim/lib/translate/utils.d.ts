import { EngineDelegate } from "paperclip";
import { FIO } from "./options";
export declare const maybeEmbed: (fromPath: string, relPath: string, engine: EngineDelegate, maxSize: number, io: FIO) => string;
