import { EngineDelegate } from "paperclip";
import { InterimCompilerOptions } from "./options";
import { InterimModule } from "../state";
export declare class InterimCompiler {
    private _engine;
    readonly options: InterimCompilerOptions;
    constructor(_engine: EngineDelegate, options: InterimCompilerOptions);
    parseFile(filePath: string): InterimModule;
}
