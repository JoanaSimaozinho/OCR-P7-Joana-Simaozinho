/// <reference types="node" />
import { EngineDelegate } from "paperclip";
import { CompilerOptions, PaperclipConfig } from "paperclip-utils";
import { InterimImport } from "..";
import { InterimAsset } from "../state/assets";
export declare type FIO = {
    readFile: (filePath: string) => Buffer;
    getFileSize: (filePath: string) => number;
};
export declare type InterimCompilerOptions = {
    cwd: string;
    config: PaperclipConfig;
    targetOptions: CompilerOptions;
    io?: FIO;
};
export declare type ModuleContext = {
    filePath: string;
    engine: EngineDelegate;
    assets: InterimAsset[];
    componentNames: string[];
    imports: InterimImport[];
    scopeIds: string[];
};
