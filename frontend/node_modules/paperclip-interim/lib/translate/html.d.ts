import { Node, Element, EngineDelegate } from "paperclip";
import { InterimElement, InterimComponent, InterimImport } from "../state";
import { ModuleContext } from "./options";
import { InterimAsset } from "../state/assets";
export declare const translateComponents: (ast: Node, filePath: string, engine: EngineDelegate, imports: InterimImport[], assets: InterimAsset[]) => InterimComponent[];
export declare const translateElement: (element: Element, context: ModuleContext) => InterimElement;
