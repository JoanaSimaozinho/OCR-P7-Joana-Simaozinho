import { VirtSheet, Node } from "paperclip-utils";
import { InterimCompilerOptions } from "./options";
import { InterimAsset } from "../state/assets";
import { EngineDelegate } from "paperclip";
export declare const getAssets: (modulePath: string, node: Node | null, sheet: VirtSheet, engine: EngineDelegate, options: InterimCompilerOptions) => InterimAsset[];
