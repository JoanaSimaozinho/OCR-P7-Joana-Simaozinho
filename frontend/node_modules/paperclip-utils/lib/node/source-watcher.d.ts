/// <reference types="node" />
import { EventEmitter } from "events";
export declare enum ChangeKind {
    Removed = 0,
    Added = 1,
    Changed = 2
}
export declare class PaperclipResourceWatcher {
    private _srcDir;
    readonly cwd: string;
    private _em;
    private _watcher;
    constructor(_srcDir: string, cwd: string);
    onChange(listener: (changeKind: ChangeKind, filePath: string) => void): () => EventEmitter;
    dispose(): void;
    private _init;
}
