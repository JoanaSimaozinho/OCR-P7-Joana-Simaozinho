import { CSSExports } from "../css/exports";
export declare type PCExports = {
    components: Record<string, ComponentExport>;
    style: CSSExports;
};
export declare type ComponentProperty = {
    name: string;
    optional: boolean;
};
export declare type ComponentExport = {
    name: string;
    properties: Record<string, ComponentProperty>;
    public: boolean;
};
