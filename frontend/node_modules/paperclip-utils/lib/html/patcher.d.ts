import { Mutation } from "./virt-mtuation";
import { VirtualNode, LoadedData } from "./virt";
import { EngineDelegateEvent } from "../core/events";
export declare const patchVirtNode: (root: VirtualNode, mutations: Mutation[]) => VirtualNode;
export declare const getVirtTarget: (mount: VirtualNode, nodePath: number[]) => VirtualNode;
export declare const updateAllLoadedData: (allData: Record<string, LoadedData>, event: EngineDelegateEvent) => Record<string, LoadedData>;
