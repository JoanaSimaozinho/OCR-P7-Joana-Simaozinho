import { CompileOptions } from "paperclip-interim";
import { CompilerOptions } from "paperclip-utils";
import { CodeCompilerOptions } from "./code-compiler";
import { DefinitionCompilerOptions } from "./definition-compiler";
export declare type CompilersOptions = {
    code: CodeCompilerOptions;
    definition: DefinitionCompilerOptions;
    extensionName: string | ((config: CompilerOptions) => string);
};
export declare const compilers: ({ code, definition, extensionName }: CompilersOptions) => ({ module, fileUrl, includes, config, targetOptions }: CompileOptions) => {
    [x: string]: string;
    ".d.ts": string;
};
