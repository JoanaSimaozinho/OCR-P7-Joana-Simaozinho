import { InterimModule } from "paperclip-interim";
import { CompilerOptions, PaperclipConfig } from "paperclip-utils";
export declare type DefinitionCompilerOptions = {
    imports: string;
    elementType: string;
};
export declare const definitionCompiler: ({ imports, elementType }: DefinitionCompilerOptions) => (module: InterimModule, filePath: string, config: PaperclipConfig, targetOptions: CompilerOptions) => string;
