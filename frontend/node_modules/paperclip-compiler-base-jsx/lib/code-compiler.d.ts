import { InterimModule } from "paperclip-interim";
import { CompilerOptions, PaperclipConfig } from "paperclip-utils";
export declare type CodeCompilerOptions = {
    imports?: string;
    preflight: string;
    vendorName: string;
};
export declare const codeCompiler: ({ preflight, imports, vendorName }: CodeCompilerOptions) => (module: InterimModule, filePath: string, config: PaperclipConfig, targetOptions: CompilerOptions, includes: string[]) => import("source-map").CodeWithSourceMap;
